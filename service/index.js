const ArticleServ = require('./article');
const CommentServ = require('./comment');
const MessageServ = require('./message');
const ReplyServ = require('./reply');
const UserServ = require('./user');

module.exports = {
  ArticleServ,
  CommentServ,
  MessageServ,
  ReplyServ,
  UserServ
};